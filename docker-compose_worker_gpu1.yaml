x-profile-config: &profile_config_volume
  type: bind
  source: ${ROOT_PATH}/config/profiles.yaml
  target: /app/config/profiles.yaml
  read_only: true

services:
  #+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++#
  #-------------------------------------------------------------------------------------------------------------#
  # Pipeline Worker Services (scalable)
  pipeline-worker-0:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_0
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=15
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped
  # healthcheck:
  #   test: [ "CMD", "nc", "-z", "rmq_Server", "5672" ]
  #   interval: 30s
  #   timeout: 10s
  #   retries: 5
  #   start_period: 40s
  #+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++#
  #-------------------------------------------------------------------------------------------------------------#
  pipeline-worker-1:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_1
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=16
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped
  # healthcheck:
  #   test: [ "CMD", "nc", "-z", "rmq_Server", "5672" ]
  #   interval: 30s
  #   timeout: 10s
  #   retries: 5
  #   start_period: 40s
  # +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++#
  # -------------------------------------------------------------------------------------------------------------#
  pipeline-worker-2:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_2
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=17
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped
  # healthcheck:
  #   test: [ "CMD", "nc", "-z", "rmq_Server", "5672" ]
  #   interval: 30s
  #   timeout: 10s
  #   retries: 5
  #   start_period: 40s
  #+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++#
  #-------------------------------------------------------------------------------------------------------------#
  pipeline-worker-3:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_3
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=18
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped
      
  pipeline-worker-4:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_4
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all  
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=19
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped
  pipeline-worker-5:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_5
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=20
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped
    

  pipeline-worker-6:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_6
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=21
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped


  pipeline-worker-7:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_7
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=22
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped    

  pipeline-worker-8:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_8
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=23
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped
  
  pipeline-worker-9:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_9
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=24
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped


  pipeline-worker-10:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_10
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=25
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped

  pipeline-worker-11:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_11
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=26
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped

  pipeline-worker-12:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_12
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=27
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped

  pipeline-worker-13:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_13
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=28
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped



  pipeline-worker-14:
    image: fr-server-pipeline_worker_service:latest
    container_name: fr_pipeline_worker_gpu1_14
    runtime: nvidia
    environment:
      - NAMESPACE=${SERVER_NAME}
      - NVIDIA_VISIBLE_DEVICES=all 
      - SERVICE_TYPE=pipeline-worker
      - PIPELINE_ID=14
      - MaxClientPerPipeline=30
      - RMQ_URL=amqp://admin:admin123@rmq_Server:5672
      - CONFIG_PROFILE=${CONFIG_PROFILE:-prod-1gpu-24gb}
      - CONFIG_PATH=/app/config/profiles.yaml
      - STORAGE_ENDPOINT=${STORAGE_ENDPOINT:-minio:9000}
      - STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY:-minioadmin}
      - STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY:-minioadmin}
      - STORAGE_SECURE=${STORAGE_SECURE:-false}
      - STORAGE_FRAMES_BUCKET=${STORAGE_FRAMES_BUCKET:-face-frames}
    volumes:
      - type: bind
        source: ${DATA_DIR}/logs/${SERVER_NAME}
        target: /app/${SERVER_NAME}/logs
      - type: bind
        source: ${ROOT_PATH}/Models_Weights
        target: /app/Models_Weights
      - type: bind
        source: ${DATA_DIR}
        target: /app/Data
      - *profile_config_volume
    networks:
      - rmq_network
      - storage_network
    restart: unless-stopped
#+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++#
#-------------------------------------------------------------------------------------------------------------#
#**************************************************************************************************************#
#**************************************************************************************************************#
networks:
  rmq_network:
    external: True
  storage_network:
    external: True
